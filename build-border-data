#!/usr/bin/env python


import argparse
from pipeline.border_pipeline import BorderDataPipeline


def read_args() -> object:
    parser = argparse.ArgumentParser(description="SwayRider border data builder")
    parser.add_argument(
            "--config",
            default="config/config.yml",
            help="Path to the config file")
    parser.add_argument(
            "--clean",
            action="store_true",
            help="Clean this pipeline's own temp and result data before running")
    parser.add_argument(
            "--clean-all",
            action="store_true",
            help="Clean all pipeline directories before running")
    parser.add_argument(
            "--tag",
            default="",
            help="Version tag (default: current date YYYY-MM-DD)")

    args = parser.parse_args()
    return args


def main():
    args = read_args()
    pl = BorderDataPipeline(args)

    if not pl.initialize():
        print("Failed to initialize border data pipeline")
        return

    if not pl.run():
        print("Failed to run border data pipeline")
        return

    print("Border data pipeline completed successfully")


if __name__ == "__main__":
    main()
